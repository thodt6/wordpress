apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: tho-haproxy
  labels:
    app.kubernetes.io/name: wordpress
spec:
  host: tho.ocp.lan
  path: "/"
  to:
    kind: Service
    name: wordpress
  port: 
    targetPort: 8080  